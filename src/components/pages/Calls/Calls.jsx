import { IconCalls, IconCheckbox, IconCheckboxChecked, IconClose, IconCross } from "../../shared/Icons";
import { ListCalls } from "./../../feature/index";
import { StyledCalls } from "./styles";

function Calls() {
  return (
      <StyledCalls>
        {/* <ListCalls /> */}
        <IconCheckboxChecked />
      </StyledCalls>
  );
}

export default Calls;